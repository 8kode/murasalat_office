# مراجعة النسخة الأخيرة — Murasalat Office v0.22.1

**النسخة المراجعة:** `v0.22.1` (آخر commit: `d7616c3` — "all donn" على فرع `Devloping-Branch2` / `develop`)
**تاريخ المراجعة:** 2026-09-10
**البيئة المستهدفة:** Frappe Framework 16.34.x + ERPNext 16.34.x

---

## الخلاصة التنفيذية

النسخة **v0.22.1** هي إصدار ترقيعي (Patch) جاء كتحصين نهائي بعد مراجعة خبير للنسخة v0.22.0.
البنية العامة **سليمة ومتسقة** مع مبدأ الحوكمة الأصلية (Frappe/ERPNext Desk هو طبقة التحكم)،
لكن المراجعة كشفت **مشكلتين حقيقيتين** تخالفان ما تدّعيه ملاحظات الإصدار نفسها — وقد تم إصلاحهما ضمن هذه المراجعة —
إلى جانب ملاحظات تصميمية يجب أخذها بالحسبان قبل الطرح على بيئة إنتاج حقيقية.

---

## ما تم فحصه

| المكوّن | العدد | النتيجة |
|---|---|---|
| DocTypes (13: منها 3 جداول فرعية) | 13 | ✅ سليمة (بعد إصلاح واحد) |
| تقارير Script Reports | 8 | ✅ سليمة |
| نقاط API مسجّلة (whitelisted) | 2 | ✅ تتحقق من صلاحية القراءة |
| Patches (ترحيل السوابق + إصلاح المخطط) | 2 + after_migrate | ✅ idempotent |
| حزمة الاختبارات الثابتة | 26 اختبار | ✅ 26/26 ناجحة (كانت 24/26) |
| JSON / ترجمة Python | الكل | ✅ صالح (بعد إصلاح واحد) |

---

## المشكلات المكتشفة (وتم إصلاحها في هذه المراجعة)

### 1. 🔴 ملف `.eslintrc` ما يزال JSON غير صالح — يخالف ملاحظات الإصدار
ملاحظات v0.22.1 تدّعي: *"Corrected `.eslintrc` JSON syntax"* — لكن الإصلاح كان **ناقصًا**:
فاصلة زائدة (trailing comma) بعد آخر قاعدة `"no-control-regex": ["off"],` قبل إغلاق القوس.
النتيجة: فشل اختبار `test_eslint_configuration_is_valid_json`، ويكسر أيضًا pre-commit hook (`check-json`).

**الإصلاح المطبَّق:** حذف الفاصلة الزائدة. الملف الآن JSON صالح ✅

### 2. 🔴 DocType العضوية يشحن مفتاح `permissions` — يخالف عقد الحوكمة
`murasalat_user_organization_membership.json` يحتوي `"permissions": []` — القيمة فارغة (لا توجد صلاحيات فعلية)،
لكن وجود المفتاح نفسه يخالف العقد المعلن: *"The app ships no ... DocPerm rows"*، ويفشل اختبار
`test_doctypes_do_not_ship_role_permission_rows`. وهو DocType الوحيد المخالف من بين 13.

**الإصلاح المطبَّق:** حذف سطر `"permissions": [],` ليطابق بقية الـ DocTypes ✅

### 3. 🟡 رابط مستند ميت في README
`README.md` يشير إلى `docs/NATIVE_ONLY_GOVERNANCE_v0.22.1.md` بينما الملف الفعلي هو
`NATIVE_ONLY_GOVERNANCE_v0.22.0.md`.

**الإصلاح المطبَّق:** تصحيح الرابط ✅

### 4. 🟡 `app_version` مفقود من `hooks.py`
الإصدار مكرر في 3 أماكن (`pyproject.toml`, `setup.py`, `__init__.py`) والمختبرات تتأكد من تطابقها،
لكن `hooks.py` لا يعرّف `app_version` — وهو المعيار الذي تقرأ منه Frappe إصدار التطبيق
(صفحة About / فحوصات التحديث). **الإصلاح المطبَّق:** إضافة `app_version = "0.22.1"` ✅

---

## نقاط قوة النسخة

1. **فصل نظيف للاهتمامات:** الكود مقتصر على سلامة البيانات (علاقات إلزامية، تفرد، تناسق تواريخ، تجزئة مرفقات) — لا يحتوي أي محرك صلاحيات أو Workflow موازٍ.
2. **التقارير آمنة الصلاحيات:** كل استعلام SQL يُقيَّد عبر `permission_condition()` المستندة إلى `frappe.get_list` (الواعية للصلاحيات) لكل DocType مكشوف (`c` و `r` معًا) — ومغطاة باختبار آلي.
3. **لا حقن SQL:** كل مدخلات المستخدم تمر عبر معاملات `%(...)s` — فحصت كل الاستعلامات يدويًا.
4. **آلية الختم وسلامة المحتوى (Integrity):** تجزئة SHA-256 تُهيَّأ تلقائيًا عند أول ختم، وتغيير المرفقات بعد الختم مرفوض برسالة واضحة، والحقول الجوهرية محمية من التعديل بعد التسجيل.
5. **ترحيل السوابق (legacy referrals):** تحويل in-place (بدون تكرار المفتاح الأساسي) مع حفظ الحقول التاريخية في `legacy_history` كـ JSON.
6. **اختبارات عقد (contract tests)** تُثبّت قرارات الحوكمة وتمنع الانحدار (regression) — وهي التي كشفت المشكلتين 1 و2 أعلاه.

---

## ملاحظات تصميمية ومخاطر (لا تمنع الإطلاق — للعلم والتخطيط)

| # | الملاحظة | الخطورة |
|---|---|---|
| 1 | **قابلية التوسع للتقارير:** `permission_condition()` تُمرِّر *كل* الأسماء المرئية في شرط `IN (...)`. مقبول لآلاف السجلات؛ مع عشرات/مئات الآلاف قد يصبح عنق زجاجة. ملاحظات الإصدار تقرّ بذلك — يُنصح باختبار حجم الإنتاج قبل الطرح المؤسسي الواسع. | 🟠 متوسطة |
| 2 | **الختم استشاري وليس مطلقًا:** مستخدم يملك صلاحية الكتابة يمكنه مسح `record_sealed_on` و `integrity_hash` وتعديل المحتوى ثم إعادة الختم. الحماية الفعلية يجب أن تأتي من Desk (Permission Levels أو Workflow) — **يُنصح بإضافتها صراحة إلى قائمة DESK_GOVERNANCE**. | 🟠 متوسطة |
| 3 | **SQL خاص بـ MariaDB/MySQL:** `CURDATE()`, `TIMESTAMPDIFF`, `DATEDIFF`, `SUM(boolean)`. لا مشكلة على Bench القياسي، لكنه يمنع أي نشر مستقبلي على PostgreSQL. | 🟡 منخفضة |
| 4 | **نطاق "My Organization" في الوارد** يتجاهل تواريخ صلاحية العضوية (`valid_from`/`valid_to`) بينما الـ DocType يتحقق منها — عدم اتساق منطقي بسيط. | 🟡 منخفضة |
| 5 | **تجزئة المرفقات** تبحث عن ملف الـ File عبر `file_url` — في حالة تكرار `file_url` قد يُختار ملف غير صحيح. حالة نادرة. | 🟡 منخفضة |
| 6 | **تقرير الإنتاجية:** الترتيب `overdue ASC` (الأقل تأخيرًا أولًا) وقد يكون المقصود عكسه، وعداد "Overdue" يشمل المكتملات المتأخرة (العنوان دقيق لكن يستحق توضيحًا للمستخدم). | 🟡 منخفضة |

---

## نتيجة الاختبارات بعد الإصلاحات

```
26 passed in 0.03s          # حزمة الاختبارات الثابتة كاملة
JSON valid (.eslintrc + membership.json + كل ملفات JSON)
Python compileall: OK
```

> **ملاحظة:** الاختبارات الثابتة لا تغطي التشغيل الفعلي — كما تذكر ملاحظات الإصدار،
> الاختبار التكاملي الكامل (runtime/UAT) يتطلب Bench حقيقي بـ Frappe 16.34.x + ERPNext 16.34.x.

---

## الخلاصة

| المعيار | الحكم |
|---|---|
| سلامة البنية والحوكمة الأصلية | ✅ ممتازة ومتسقة |
| مطابقة الكود لادعاءات ملاحظات الإصدار | ⚠️ كانت مخالفة في نقطتين — **أُصلحتا** |
| جاهزية الإطلاق التجريبي (بعد الإصلاحات) | ✅ جاهزة |
| جاهزية الإنتاج واسع النطاق | 🟠 مشروطة بالتحقق من أداء التقارير على حجم البيانات الفعلي + ضبط ختم السجلات من Desk |
