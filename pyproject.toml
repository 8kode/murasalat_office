[project]
name = "murasalat_office"
version = "0.22.1"
description = "Metadata-first correspondence management for Frappe"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
